# 质量保障体系梳理(会议纪要整理)

> 整理自会议录音转写稿 + 白板照片。转写稿为语音识别结果,个别专有名词可能不准确,已用「?」标注存疑处。

## 一句话结论

要产出**一份文档 + 一张端到端大图**,内容分两大部分:

1. **机制建设(框架)**:先讲清楚四大理念,再逐个机制写"现状覆盖情况 → 差距"(下一步计划由领导补写);
2. **端到端流程控制点**:沿"需求 → 设计 → 编码 → 测试 → 发布/运维"逐阶段写"现有控制点 → 问题差距 →(方案)"。

当前整体判断:**大错没有,小错不断**。P0/P1 已降下来,但交付给业务的东西仍然粗糙,要靠体系而不是靠人来改变。

---

## 第一部分:机制建设(框架)

### 四大理念(白板 ①~④)

| # | 理念 | 含义 |
|---|------|------|
| 1 | 可观测(观测 + 预警) | 看得到问题,才谈得上恢复 |
| 2 | 可快速恢复 | 快速响应机制 + 快速恢复机制;与高可用建设配套 |
| 3 | 灰度控制影响面 | 通过灰度发布控制变更影响范围 |
| 4 | 质量持续运营 | 把质量当成项目维度来做(质量虚拟化),持续运营 |

### 现有机制盘点

- **可观测/预警**:已有可观测管理列表(在质检与数仓侧);
- **快速响应机制**:通过工单/值班等机制运转;
- **快速恢复机制**:在高可用之下单独拆出,含高可用本身建设 + 快速恢复的结构;
- **灰度机制**:需要打一张表,盘点哪些系统已构建灰度能力;
- **框架建设**:基于平台的统一框架;
- **微服务化**:属于基本概念,**不用写**。

### 写作要求

- 先把**原则(理念)写清楚**,再写每个机制;每个机制不用写很多,写清楚**当前建设/覆盖情况**即可;
- 机制覆盖的系统要写清楚,检查有无遗漏;
- 系统范围:**只写 S 级系统**,按大领域列出主要 S 级系统(如客户市场、客服、国际等各是哪几个,写个大概即可);A 级简单带过,还在开发中的不用写;
- **差距要列出来**:如高可用尚未建完、错误码运维推进到什么程度、差在哪里;
- **下一步计划不用写**,由(会议主持人)统一补写。

---

## 第二部分:端到端流程控制点

沿流程逐阶段写:现有控制点(现状)→ 问题/差距 →(方案:已知的自己写,不知道的由他人补)。

### 0. 业务设计方案(需求之前需补充的环节)

- 目前缺失,**李敏在写**业务设计方案(方案怎么做好、业务方案模板);
- 本次讨论暴露的很多问题根源就在"业务方案没做"。

### 1. 需求

- **现状**:已有需求评审;
- **问题**:
  - 需求写不清楚,导致开发理解偏差、走偏;
  - 产品考虑不足(很多场景产品想不到);
  - 目前需求还没法很好地直接转化为高质量代码;
- **后续方向**:AI 自动化还原/生成(?)是另一个专项。

### 2. 设计

- **现状**:已有内部设计评审;
- **最大问题**:很多产品**缺少一个大的整体蓝图/未来规划**。导致设计只为快速满足眼前需求,后面发现不可扩展只能反复改(如 CSDP、CDD 的设计,一堆迭代一直在改)。

### 3. 编码(AI 编码)

- **现状**:有 Code Review 机制,核心模块有做 CR;有自测要求;
- **问题**:
  - AI 编码产出代码量太大,**CR 看不过来**,评审能力跟不上;
  - 部署/考虑不足(白板);
  - 开发对业务理解不够;合作方开发"不懂也不问",需求写什么就开发什么,需求一旦没写好开发必然走偏(问题回推到需求环节);
  - 自测有差距;
- **分工**:此部分由**宏卫**来写,方案已讨论准备好。

### 4. 测试

- **核心问题:不做产品验证 + 基础数据缺失,没有全场景验证**;
- 具体表现:
  - 测试账号体系没有提前打通:业务/测试账号用不了、没有权限;
  - 测试环境与测试账号体系建设没有提前搞好;
  - 基础数据缺失:没有给业务准备好数据,业务过来自己折腾半天数据也跑不通(OTC 项目最突出);
  - 缺少产品验证环节(OVC(?)项目上线前补做产品验证,立刻发现了问题);
- **现状**:自测有、可以写上,差距也要写。

### 5. 发布 / 运维

- **现状**:有发布流程,含体验、业务验收等环节;
- **最突出问题**:**发布管控不住、影响面无法识别**——发布影响面分析不清,导致发布后影响了上下游别人却不知道;
- (千流(?)发布平台的话题本次先不展开。)

---

## 已总结的典型问题清单(小明已汇总)

1. 需求理解有偏差,做出来的东西在开发编码环节就断了;
2. 开发缺乏业务全局视角;
3. 改动影响面分析缺失;
4. SIT 环境问题较多;测试只做了业务验证,没有做生产级验证;
5. 测试人员权限不对;
6. 产品漏测;
7. UAT(?)阻塞:基础数据缺失;
8. 体验问题。

> 关于 UAT 数据问题的讨论:目前完全靠人在兜底,如果有人盯就不出问题、没人盯就出问题——恰恰说明缺机制,必须做成强制的前置认证/检查。

---

## 输出物与分工

| 事项 | 负责人 | 说明 |
|------|--------|------|
| 机制建设(理念 + 框架 + 覆盖情况 + 差距) | 宏卫 | 按"原则 → 机制 → 覆盖情况 → 差距"来写;只盘 S 级系统 |
| 端到端流程控制点(含一张端到端大图) | 伟斌 | 流程部分可画图(矩阵式大图);已写的细节内容归入对应阶段即可 |
| 业务设计方案 | 李敏 | 需求之前的环节 |
| 问题清单 | 小明 | 已总结,会前再同步一遍 |
| 各阶段方案 | 各自 | 已有方案就写,缺的由他人补 |
| 下一步计划 | 会议主持人 | 大家不用写 |

**时间要求**:明天上午理完;约上午 11 点左右过去一起对齐,再把问题清单过一遍。

---

## 附:白板结构速记

```
业务的设计方案 ──┐
                 │        一、框架
① 可观测         │        [需求] ── [设计] ── [测试] ── [发布]
② 可快速恢复     │                                        │
③ 灰度控制影响面 │        ① 机制:{ 可观测/灰度 }          [运维]
④ 持续运营       │                 高可用(不宕机){ 快速响应 / 框架 }
                 │        ② 流程(控制点)
                 │        二、演示 demo
                 │        2. 设计
                 │        3. 编码(AI)(Coding)  部署考虑不足
                 │        4. 测试:(数据)
```
